broader_and_partof.query;Elements that have skos:broader and rdf:partOf relation to the same element;error;
has_no_path_to_top.query;Elements that are not connected to any top level element;hint;
has_not_exactly_one_en_preflabel.query;Elements that have not exactly one english skos:prefLabel;error;
notation_duplicate.query;Two Elements with the same skos:notation;error;
